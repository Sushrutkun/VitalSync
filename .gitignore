# graphify outputs
# Keep graph.json, GRAPH_REPORT.md, graph.html, manifest.json — others reuse for token-saving queries
# Ignore extraction cache (large, regenerable) + personal cost tracker
graphify-out/cache/
graphify-out/cost.json
graphify-out/.graphify_*
